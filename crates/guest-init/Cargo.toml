[package]
name = "atelier-guest-init"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "atelier-guest-init"
path = "src/main.rs"

[dependencies]
nix = { version = "0.31.3", features = ["mount", "process"] }
